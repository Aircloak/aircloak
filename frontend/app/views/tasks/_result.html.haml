-# Includes additional, page-specific javascripts
= content_for(:javascript_include) {javascript_include_tag "tasks/result", "data-turbolinks-track" => true}

%hr

-# set rendering parameters
.render_params{data: {result: @result.to_json, task_last_update: @task.code_timestamp.to_i * 1000}}

%p
  %b time:
  %span#time
%p
  %b errors:
  %span#errors

%p
  %b properties:
  %table
    %tr
      %td
        %table.table.table-striped.table-centered#result_table
          %thead
          %tbody
      %td
        %div#controls.hidden
          %input{:type => "radio", :name => "chart_type", :checked => "checked", :value => "bar", :style => "margin-left: 50px;"}
          %b Bar chart
          %input{:type => "radio", :name => "chart_type", :value => "line", :style => "margin-left: 50px;"}
          %b Line chart
        %div#charts

%hr
