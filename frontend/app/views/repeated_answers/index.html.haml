- if @unresolved.count > 0
  %h3 Reported Unresolved Repeated Answers

  %table.table.table-striped
    %thead
      %tr
        %th Time (UTC)
        %th Analyst
        %th Cluster
        %th Source-IP
        %th Bucket Label and Value
        %th Bucket Count
        %th Noise Standard Deviation
        %th
    %tbody
    - @unresolved.each do |answer|
      %tr
        %td= link_to answer.timestamp_in_UTC, repeated_answer_path(answer.id)
        %td= answer.analyst.name
        %td= answer.cluster_name
        %td= answer.source_ip
        %td= answer.bucket_label_and_value
        %td= answer.bucket_count
        %td= answer.noise_sd
        %td= link_to 'View', repeated_answer_path(answer.id)
- else
  %h3 No unresolved repeated answers stored in the database.


- if @resolved.count > 0
  %h3 Reported Resolved Repeated Answers

  %table.table.table-striped
    %thead
      %tr
        %th Time (UTC)
        %th Analyst
        %th Cluster
        %th Source-IP
        %th Bucket Label and Value
        %th Bucket Count
        %th Noise Standard Deviation
        %th
    %tbody
    - @resolved.each do |answer|
      %tr
        %td= link_to answer.timestamp_in_UTC, repeated_answer_path(answer.id)
        %td= answer.analyst.name
        %td= answer.cluster_name
        %td= answer.source_ip
        %td= answer.bucket_label_and_value
        %td= answer.bucket_count
        %td= answer.noise_sd
        %td= link_to 'View', repeated_answer_path(answer.id)
