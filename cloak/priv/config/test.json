{
  "data_sources": {
    "local": {
      "salt": "test salt which doesn't matter since tests have SD=0",
      "driver": "postgresql",
      "parameters": {
        "hostname": "localhost",
        "username": "postgres",
        "database": "cloaktest1",
        "sync_connect": true,
        "pool_size": 2
      },
      "tables": {
      }
    }
  }
}
