{
  "salt": "test salt which doesn't matter since tests have SD=0",
  "data_sources": {
    "local": {
      "driver": "postgresql",
      "parameters": {
        "hostname": "localhost",
        "username": "postgres",
        "database": "cloaktest1",
        "sync_connect": true,
        "pool_size": 2
      },
      "tables": {
      }
    },
    "local_odbc": {
      "driver": "odbc",
      "parameters": {
        "connect": "DSN=PostgreSQL;UID=postgres;Database=cloaktest1;Server=localhost;",
        "pool_size": 2
      },
      "tables": {
      }
    }
  }
}
