{
  "salt": "test salt which doesn't matter since tests have SD=0",
  "data_sources": {
    "local": {
      "driver": "postgresql",
      "parameters": {
        "hostname": "localhost",
        "username": "postgres",
        "database": "cloaktest1",
        "sync_connect": true,
        "pool_size": 2
      },
      "tables": {
      }
    }
  }
}
