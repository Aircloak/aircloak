{
  "salt": "test salt which doesn't matter since tests have SD=0",
  "data_sources": {
    "local": {
      "driver": "postgresql",
      "parameters": {
        "hostname": "localhost",
        "username": "postgres",
        "database": "cloaktest1"
      },
      "tables": {
      }
    },
    "local_odbc": {
      "driver": "odbc",
      "parameters": "DSN=PostgreSQL;UID=postgres;Database=cloaktest1;Server=localhost;",
      "tables": {
      }
    }
  }
}
