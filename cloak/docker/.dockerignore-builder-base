# ignore everything by default
*

# Whitelist only needed folders


# This is the bare minimum needed to build the deps
!cloak/VERSION
!cloak/Makefile
!cloak/mix.exs
!cloak/mix.lock
!cloak/config
!cloak/fetch_deps.sh
!common


# Additional ignores

# We don't want runtime config included. It needs to be provided through a
# mounted volume
cloak/priv/config
