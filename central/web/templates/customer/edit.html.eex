<ol class="breadcrumb">
  <li><%= link "Customers", to: customer_path(@conn, :index) %></li>
  <li class="active"><%= @changeset.data.name %></li>
</ol>

<h2>Edit <%= @changeset.data.name %></h2>

<%= render "_form.html", changeset: @changeset,
  action: customer_path(@conn, :update, @changeset.data.id), conn: @conn %>
