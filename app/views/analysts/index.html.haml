%h1 Listing analysts

- if @analysts.size > 0 then
  %table.table.table-striped
    %thead
      %tr
        %th Name
        %th Clusters
        %th

    %tbody
      - @analysts.each do |analyst|
        %tr
          %td= analyst.name
          %td= analyst.clusters.count
          %td
            = link_to 'Show', analyst, class: "btn btn-mini"
            = link_to 'Edit', edit_analyst_path(analyst), class: "btn btn-mini"
            = link_to 'Destroy', analyst, :method => :delete, :data => { :confirm => 'Are you sure?' },
                class: "btn btn-mini btn-danger" if analyst.can_destroy?

%h4 Add a new analyst
= render 'form'
