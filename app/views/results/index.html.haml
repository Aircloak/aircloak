- if permitted_to? :read, :tasks
  %h1 Tasks

  - unless @tasks.count == 0
    %p Select a task you want to see

    %table{:class => "table table-striped"}
      %tr
        %th Task
        %th Number of stored results
        %th
        %th
      - @tasks.each do |task|
        %tr
          %td= link_to task.name, result_path(task.id)
          %td= link_to task.results.count, result_path(task.id)
          %td= link_to 'Execute batch', execute_as_batch_task_task_path(task), method: "post", class: "btn btn-mini" unless task.stored_task
          %td= link_to 'Edit', edit_task_path(task), class: "btn btn-mini"

  - else
    %p There are no tasks to show results for.
