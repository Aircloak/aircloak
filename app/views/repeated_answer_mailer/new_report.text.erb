Repeated answers detected for analyst <%= @report.analyst.name %> on cluster <%= @report.cluster.name %>
========================================================================================================

  - task started from <%= @report.source_ip %> at <%= @report.timestamp_in_UTC %>
  - bucket <%= @report.bucket_label_and_value %> with noisy-count <%= @report.bucket_count %>
  - noise standard deviation <%= @report.noise_sd %>
  - there are <%= RepeatedAnswer.where(resolved: false).count %> unresolved reports in total

<% @task_codes.each do |task_code| %>

  <% if task_code.id == @task_codes.first.id %>
    Generated by
    ------------
  <% else %>
    ------------
  <% end %>

  <%= task_code.prefetch %>

  Trustworthy: <%= task_code.trustworthy %>

  <% task_code.ra_library_code_ra_task_codes.each do |connector| %>

    <%= "Library: #{connector.name}" %>

    <%= connector.ra_library_code.code %>

  <% end %>

  <%= task_code.code %>
<% end %>
