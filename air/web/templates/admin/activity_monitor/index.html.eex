<h2>Activity monitor</h2>

<div id="activity-monitor-view" />

<script>
  window.pageConfig = {
    onLoad: function(App) {
      var props = {
        CSRFToken: "<%= @csrf_token %>",
        guardianToken: "<%= @guardian_token %>",
        userId: "<%= @current_user.id %>"
      };

      App.activityMonitor(props, document.getElementById("activity-monitor-view"));
    }
  };
</script>
