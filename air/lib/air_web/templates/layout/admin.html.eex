<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <%= if @conn.assigns[:refresh] do %>
      <meta http-equiv="refresh" content="1;url=<%= @conn.assigns[:refresh] %>">
    <% end %>

    <title>Aircloak – Admin - Air control</title>
    <link rel="shortcut icon" href="<%= static_path(@conn, "/frontend/images/favicon.ico") %>" type="image/x-icon">
    <link rel="stylesheet" href="<%= static_path(@conn, "/frontend/app.css") %>">
  </head>

  <body>
    <div class="body-wrapper">
      <div class="container-fluid">
        <%= render "_navbar.html", conn: @conn, admin: true %>

        <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
        <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
        <nav class="navbar navbar-vertical fixed-left navbar-expand-md navbar-light bg-light border-right">
          <div class="container-fluid">
            <div class="collapse navbar-collapse" id="sidebarCollapse">
              <ul class="navbar-nav">
                <%= warning_sidebar_link(@conn) %>

                <%= sidebar_link(@conn, "Activity monitor", "chart-line", admin_activity_monitor_path(@conn, :index)) %>
                <%= if audit_log_enabled?() do %>
                  <%= sidebar_link(@conn, "Audit log", "stream", admin_audit_log_path(@conn, :index)) %>
                <% end %>
                <%= sidebar_link(@conn, "Cloaks", "user-secret", admin_cloaks_path(@conn, :index)) %>
                <%= sidebar_link(@conn, "Data sources", "database", admin_data_source_path(@conn, :index)) %>
                <%= sidebar_link(@conn, "Analysis", "signal", admin_analysis_path(@conn, :index)) %>
                <%= sidebar_link(@conn, "Failed queries", "bomb", admin_query_path(@conn, :failed)) %>
              </ul>
              <hr class="navbar-divider my-3" />
              <ul class="navbar-nav">
                <%= sidebar_link(@conn, "Users", "user", admin_user_path(@conn, :index)) %>
                <%= sidebar_link(@conn, "Groups", "user-friends", admin_group_path(@conn, :index)) %>
              </ul>
              <hr class="navbar-divider my-3" />
              <ul class="navbar-nav">
                <%= sidebar_link(@conn, "Settings", "cogs" ,admin_settings_path(@conn, :show)) %>
                <%= sidebar_link(@conn, "Privacy policy", "user-shield", admin_privacy_policy_path(@conn, :index)) %>
                <%= sidebar_link(@conn, "Aircloak license", "key", admin_license_path(@conn, :edit)) %>
              </ul>
            </div>
          </div>
           </nav>

          <main>
            <div class="container-fluid">
              <%= render @view_module, @view_template, assigns %>
            </div>
          </main>

      </div> <!-- /container -->

      <div class="admin-footer"><%= render AirWeb.SharedView, "footer.html", conn: @conn %></div>
    </div>

    <script src="<%= static_path(@conn, "/frontend/app.js") %>"></script>
  </body>
</html>
