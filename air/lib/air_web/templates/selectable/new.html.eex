<%= render "_form.html", Map.merge(assigns, %{
  view_name: "New #{selectable_kind_name(@kind)}",
  action_name: "Create",
  action: data_source_selectable_path(@conn, :create, @data_source.name, @kind)
}) %>
