= help_link ['introduction-queries', 'task-execution', 'understanding-the-query-model', 'lookup-tables', 'query-library']

%h3 Editing task

= render 'form'

= link_to 'Tasks overview', tasks_path
= link_to 'Latest results', latest_results_task_path(@task.token), class: "right-aligned" if @task.token
