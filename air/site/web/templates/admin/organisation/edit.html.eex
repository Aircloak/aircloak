<h2>Edit organisation</h2>

<%= render "form.html", changeset: @changeset,
  action: admin_organisation_path(@conn, :update, @organisation) %>

<%= link "Back", to: admin_organisation_path(@conn, :index) %>
